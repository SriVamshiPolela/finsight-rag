# Phase 1 - ingestion & chunking
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2
langchain-text-splitters>=0.2
pydantic>=2.7
python-dotenv>=1.0

# Phase 2 - embeddings & vector store
sentence-transformers>=3.0
faiss-cpu>=1.8
pinecone>=5.0
mlflow>=2.14

# Phase 3 - multi-agent orchestration
# (langchain itself is used via langchain-text-splitters above; llama-index
# was evaluated but not needed since raw parsing + those splitters sufficed
# for Phase 1 - see README)
langgraph>=0.2
anthropic>=0.30
openai>=1.30

# Phase 4 - evaluation
ragas>=0.1
datasets>=2.20

# Phase 5 - API
fastapi>=0.111
uvicorn[standard]>=0.30

# Phase 6 - testing / CI
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.5
